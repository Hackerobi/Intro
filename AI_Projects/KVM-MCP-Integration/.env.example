# KVM MCP Server Configuration
# Copy this to .env and adjust for your environment

# Libvirt connection URI (default: qemu:///system for local KVM)
# KVM_MCP_LIBVIRT_URI=qemu:///system

# Default directory for VM disk images
# KVM_MCP_DISK_PATH=/var/lib/libvirt/images

# Default directory to search for ISO images
# KVM_MCP_ISO_PATH=/var/lib/libvirt/images

# Default libvirt network for new VMs
# KVM_MCP_DEFAULT_NETWORK=default

# Default VM resources
# KVM_MCP_DEFAULT_MEMORY=2048
# KVM_MCP_DEFAULT_VCPUS=2
# KVM_MCP_DEFAULT_DISK_SIZE=40
